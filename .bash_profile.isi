#!/usr/bin/env bash

export GOCACHE=/nas/gaia/users/berquist/.cache/go-build

[[ -f ~/.bashrc ]] && source ~/.bashrc

if [[ "$TERM" != "" ]]; then
    stty -ixon
fi

export GIT_AUTHOR_EMAIL='berquist@isi.edu'
export GIT_COMMITER_EMAIL='berquist@isi.edu'
