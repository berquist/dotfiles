#!/usr/bin/env zsh

# ???

########################################
### hydrogen-specific things
########################################

export QCPROGS=${HOME}/data/opt/apps
export apps=${QCPROGS}
export SCRATCH=/tmp
export scratch=${SCRATCH}

eval $(keychain --nogui --eval --quiet --agents ssh id_rsa)

alias doupgrade='yaourt -Syyua --devel'

########################################
### MODULES
########################################
